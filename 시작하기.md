# 쇼츠 제작소

MoneyPrinterTurbo를 한글화하고, 공감툰처럼 왼쪽 설정 / 오른쪽 미리보기 화면으로 바꾼 버전입니다.

## 한 번만 하면 되는 일

1. 이 폴더에서 아래를 실행합니다.

```bash
sh webui.sh
```

2. 브라우저가 열리면 주소는 보통 http://127.0.0.1:8501 입니다.
3. 오른쪽 위 **설정**에서 AI 모델 API 키를 넣습니다.
4. 왼쪽에 주제를 적고 **쇼츠 만들기**를 누릅니다.

기본 화면 비율은 세로 쇼츠 9:16, 기본 언어는 한국어입니다.

영상을 실제로 만들려면 설정에 모델 키가 필요합니다. 목소리(Edge TTS)는 키 없이 쓸 수 있습니다.
